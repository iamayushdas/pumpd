{
  "name": "pumpd",
  "version": "1.2.4",
  "private": true,
  "description": "Self-hosted gym & bodyweight tracker",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "npm --prefix api install --omit=dev && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node api/server.js",
    "dev": "bash start.sh",
    "setup": "bash setup.sh",
    "migrate": "cd api && node migrate-exercises.js",
    "test": "cd frontend && npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/DuarteSantos8/pumpd"
  }
}
